1. The script should take the log file as input, and display the top 10 IPs making the most requests, display the IP address and number of requests made. Indicate if this is done using the option below.
b. For each of the top 10 IPs, show the top 5 pages requested and the number of requests for each. Indicate if this is done using the option below.
c. Percentage of unsuccessful requests. Indicate if this is done using the option below.
